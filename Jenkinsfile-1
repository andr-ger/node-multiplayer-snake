pipeline {
  agent { label 'built-in' }
  options {
    buildDiscarder(logRotator(numToKeepStr: '5'))
  }
  stages {
    stage('Build') {
      steps {
        docker build -t andrger/snake:latest .
      }
    }
    stage('Scan') {
      steps {
        trivy image andrger/snake:latest
      }
    }
  }
}
