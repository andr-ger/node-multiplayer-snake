pipeline {
  agent { label 'built-in' }
  options {
    buildDiscarder(logRotator(numToKeepStr: '5'))
  }
  stages {
    stage('Build') {
      steps {
        sh 'docker build -t andrger/snake .'
      }
    }
    stage('Scan') {
      steps {
        sh 'trivy andrger/snake'
      }
    }
  }
}
