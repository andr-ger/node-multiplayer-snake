pipeline {
  agent { label 'Ubuntu-App-Server' }
  options {
    buildDiscarder(logRotator(numToKeepStr: '5'))
  }
  stages {
    stage('Build') {
      steps {
        sh 'docker build -t andrger/snake:latest .'
      }
    }
    stage('Scan') {
      steps {
        sh 'trivy andrger/snake:latest'
      }
    }
  }
}
